<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.example.urbanfix">
    <!-- Debug manifest overlay: points the debug app to the network security config
         that permits cleartext traffic to localhost/10.0.2.2. This manifest is
         merged only into debug builds. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <application android:networkSecurityConfig="@xml/network_security_config" />
</manifest>
