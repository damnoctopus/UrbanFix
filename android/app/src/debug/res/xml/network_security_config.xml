<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow cleartext (HTTP) traffic for debug builds so the emulator
         can communicate with a local development server. Remove or lock
         this down for production. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
